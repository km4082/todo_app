<h1>編集しましょう</h1>

<%= form_with(model: @todo) do |f| %>
    <div>
        <%= f.label :description, '中身' %><br>
        <%= f.text_area :description %>
    </div>
    <div>
        <%= f.submit '更新する' %>
    </div>
<% end %>

<%= link_to '一覧に戻る', todos_path %>